{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Capability for the main window on mobile (iOS / Android). No fs permissions: ACP UI on mobile only talks to remote agents over WebSocket, and the bridge replies -32601 to fs/* RPCs.",
  "windows": ["main"],
  "platforms": ["iOS", "android"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    "dialog:default"
  ]
}
